namespace Application.Constants.Identity;

public static class RoleConstants
{
    public const string AdminRoleName = "Admin";
    public const string AdminRoleDescription = "Global administrator role with all permissions";
    
    public const string DefaultRoleName = "Default";
    public const string DefaultRoleDescription = "Default role with base permissions, granted to every account by default";
}